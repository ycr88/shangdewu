{# TODO
[] read the file
[x] trim values
[] check the format
#}
{%- macro render_field(data, task) %}
{% set output = { 'task': task.name ,'url': task.url,'data': data}%}
{{ output|json_encode(constant('JSON_PRETTY_PRINT') b-or constant('JSON_UNESCAPED_SLASHES') b-or constant('JSON_UNESCAPED_UNICODE'))}}
{%- endmacro %}
{%- import _self as macro %}
{%- autoescape false %}
{% set task = {'name': uri.basename , 'url': uri.url} %}
{{- macro.render_field(form.data, task) }}
{%- endautoescape %}
