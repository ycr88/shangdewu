{# TODO
[] read the file
[] trim values
[] check the format

#}
{%- macro render_field(data) %}
{% set output = {'id':random(), 'data': data }%}
{{ output|json_encode(constant('JSON_PRETTY_PRINT') b-or constant('JSON_UNESCAPED_SLASHES') b-or constant('JSON_UNESCAPED_UNICODE'))}}
{%- endmacro %}
{%- import _self as macro %}
{%- autoescape false %}
{{- macro.render_field(form.data) }}
{%- endautoescape %}
